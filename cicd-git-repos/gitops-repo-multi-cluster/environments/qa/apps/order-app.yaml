apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: order-service-qa
  namespace: argocd
spec:
  destination:
    namespace: default
    server: https://35.224.24.120   # AKS cluster
  sources:
    - path: order-app-chart
      repoURL: https://github.com/ramanujds/gitops-repo-multi-cluster
      targetRevision: HEAD
      helm:
        releaseName: order-service
        valueFiles:
          - $values/environments/qa/values/order-values.yaml
    - repoURL: https://github.com/ramanujds/gitops-repo-multi-cluster
      targetRevision: HEAD
      ref: values
  project: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      enabled: true